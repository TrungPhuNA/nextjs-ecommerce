import Master from './../layouts/Master'
import {getProducts} from '../api/home'
import {Card} from 'react-bootstrap';
const Home = ({ posts }) => {
    return (
        <Master>

        </Master>
    )
}

export default Home
